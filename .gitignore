node_modules/
/dump/
dump.rdb
*.swp
dist/
coverage/
.wrangler/
.vitest-cache/
*.log
.claude/
# Local-only secrets for `wrangler dev` / Miniflare. See CLAUDE.md §3.1.
.dev.vars
# Playwright e2e outputs
playwright-report/
test-results/
node_modules/.playwright/
# Curated assets dir — rebuilt from sources by scripts/build-assets.sh.
# Commit-ignored because it contains built JS (client/dist/player.js,
# client-multi/dist/**) that goes stale. CI regenerates it before
# `wrangler deploy --dry-run`. See CLAUDE.md §8 Phase 4.1/11.
/assets/
# Phase 12 mutation-testing outputs (nightly StrykerJS). Reports are
# uploaded as CI artifacts, not committed. See docs/MUTATION_REPORT.md.
reports/
.stryker-tmp/
stryker.log
# `inPlace: true` writes this vitest setup helper next to sources;
# Stryker deletes it post-run but a killed / interrupted run can leave it.
stryker-setup-*.js

# Workerd bundle output (scripts/build-workerd-bundle.sh regenerates)
packages/worker/workerd/worker/
packages/worker/workerd/assets
packages/worker/workerd/do-storage/
packages/worker/workerd/config.runtime.capnp
packages/worker/workerd/override.capnp

# Docker-compose bind-mount volume
ethercalc-data/
