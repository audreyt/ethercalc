{
  "name": "@ethercalc/migrate",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 11b — Stream a legacy Redis (RESP) into the new Worker via PUT /_migrate/seed/:room (§12)",
  "license": "CC0-1.0",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./apply": "./src/apply.ts",
    "./sources/redis": "./src/sources/redis-source.ts",
    "./targets/http": "./src/targets/http.ts",
    "./targets/in-memory": "./src/targets/in-memory.ts",
    "./resp-client": "./src/resp-client.ts",
    "./cli": "./src/cli.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsgo --noEmit",
    "migrate": "bun run src/cli.ts",
    "mutation": "stryker run"
  },
  "dependencies": {
    "@ethercalc/shared": "workspace:*"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^22.9.0",
    "@vitest/coverage-istanbul": "^4.1.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
