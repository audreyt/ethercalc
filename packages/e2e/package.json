{
  "name": "@ethercalc/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the EtherCalc TypeScript rewrite",
  "license": "CC0-1.0",
  "type": "module",
  "scripts": {
    "pretest": "vp exec playwright install chromium",
    "test": "vp exec playwright test",
    "test:single": "vp exec playwright test --project=chromium",
    "test:headed": "vp exec playwright test --headed",
    "test:ui": "vp exec playwright test --ui",
    "install:browsers": "vp exec playwright install --with-deps chromium",
    "typecheck": "tsc --noEmit",
    "report": "vp exec playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.9.0",
    "wait-port": "^1.1.0"
  }
}
